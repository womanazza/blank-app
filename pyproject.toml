[project]
name = "blank-app"
version = "0.1.0"
description = "A simple Streamlit app template."
readme = "README.md"
requires-python = ">=3.11,<3.14"
dependencies = [
    "streamlit>=1.61.1",
    "requests",
    "beautifulsoup4",
    "pandas",
]

[tool.uv]
package = false